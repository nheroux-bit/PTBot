# PTBot Project Guidelines

Legend (from RFC2119): !=MUST, ~=SHOULD, ≉=SHOULD NOT, ⊗=MUST NOT, ?=MAY.

**deft_version**: 0.15.0

Only specify items here that **override or extend** the deft defaults.

**Reference**: `deft/main.md` | [python.md](deft/languages/python.md)

## Project Configuration

**Tech Stack**: CLI using Python, `oz agent run` for agent orchestration, `fpdf2` for PDF generation, `openpyxl` for Excel output

## Strategy

Use [interview](deft/strategies/interview.md) for this project.

## Workflow

```bash
task check         # Pre-commit gate
task test:coverage # Coverage target
task build         # Build project
task clean         # Clean artifacts
```

## Standards

**Quality:**
- ! Run `task check` before every commit
- ! Achieve ≥85% coverage overall + per-module
- ! Store secrets in `secrets/` dir
- ~ Provide `.example` templates for secrets

## Project-Specific Rules

- ! Reuse `run_local_agent()` from attack.market's orchestrator — do not duplicate agent-spawning logic
- ! All financial figures in deliverables must carry source attribution
- ! Two-pass pipeline: discovery fan-out → compile/filter → deep-dive fan-out → QC
- ~ Excel output follows IB formatting conventions from financial-analyst skill

## Branching

Allow direct commits to master: true

---

**Generated by**: deft-setup skill
**Date**: 2026-05-01
